{
  "name": "termosalud-clone",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/process-html.mjs && vite build",
    "preview": "vite preview",
    "serve": "node scripts/serve.mjs"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "nodemailer": "^10.0.0"
  }
}
